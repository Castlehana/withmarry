export function NotFoundPage() {
  return (
    <p
      style={{
        margin: 0,
        minHeight: "100vh",
        display: "grid",
        placeItems: "center",
        padding: "1rem",
        fontFamily: "system-ui, sans-serif",
        fontSize: "1rem",
        color: "#333",
        textAlign: "center",
      }}
    >
      요청하신 페이지를 찾을 수 없습니다.
    </p>
  );
}
